import React, { Component } from 'react';

class App extends Component {
	render() {
		return (
			<div>
				<h1>React Boilerplate</h1>
				<p>React webpack and babel starter created from scratch</p>
				<p>
					run command <code>npm install</code> to use this boilerplate in order to create an React App from
					scratch without all junk.
				</p>
			</div>
		);
	}
}
export default App;
